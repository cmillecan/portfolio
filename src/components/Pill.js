import styled from "styled-components";

const Pill = styled.div`
  width: auto;
  border-radius: 25px;
  padding: 4px 12px;
  font-size: 14px;
  font-weight: lighter;
  margin-right: 5px;
  margin-bottom: 5px;
  color: var(--white);
  background-color: var(--lightbrown); 
 )
`;

export default Pill;
